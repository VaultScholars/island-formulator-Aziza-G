<% content_for :title, "Ingredients" %>

<div class="container mx-auto mt-10 px-5">
  <div class="flex justify-between items-center mb-10">
    <h1 class="text-4xl font-extrabold text-white">My Ingredient Shelf</h1>
    <%= link_to "Add New Ingredient", new_ingredient_path, 
        class: "bg-rose-900 hover:bg-white text-white hover:text-rose-800 font-bold py-3 px-6 rounded-lg  shadow-md transition duration-200" %>
  </div>

  <div id="ingredients" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% @ingredients.each do |ingredient| %>
      <div class="bg-orange-100 shadow-lg rounded-xl p-6 border border-gray-100 hover:shadow-xl transition-shadow duration-300">
        <div class="mb-4">
          <span class="inline-block px-3 py-1 text-xs font-semibold tracking-wide text-pink-800 uppercase bg-pink-100 rounded-full">
            <%= ingredient.category %>
          </span>
        </div>
        <h2 class="text-xl font-bold text-rose-800 mb-2"><%= ingredient.name %></h2>
        <p class="text-yellow-600 line-clamp-3 mb-4"><%= ingredient.description %></p>
        
        <div class="mt-auto pt-4 border-t border-gray-100 flex justify-between items-center">
          <%= link_to "View Details", ingredient, class: "text-pink-800 font-medium hover:text-pink-600 transition-colors" %>
          <span class="text-gray-700 text-sm">#<%= ingredient.id %></span>
        </div>
      </div>
    <% end %>
  </div>
</div>
